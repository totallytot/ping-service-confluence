<html>
    <head>
        <title>Old Pages Review Service Configuration</title>
        <meta charset="UTF-8"/>
        <meta name="decorator" content="atl.admin">
    </head>
    <body>
        <form id="admin" class="aui" action="" method="POST">

            <div class="field-group">
                <label for="name">Select space key to be monitored:</label>
                <select class="multi-select" multiple="multiple" size="8" name="allSpaceKeys" id="allSpaceKeys">
                        #foreach($space in $allSpaceKeys)
                            <option value=$space>
                        #end
                    </select>
            </div>

            <div class="field-group">
                <label for="name">Select affected groups:</label>
                <select class="multi-select" multiple="multiple" size="8" name="allGroups" id="allGroups">
                    #foreach($group in $allGroups)
                        <option value="$issueType.get('name')">
                    #end
                </select>
            </div>

            <div class="field-group">
                <label for="name">Period to consider pages as outdated (day(s)):</label>
                <input type="number" min="1" id="timeframe" name="timeframe" class="text">
            </div>

            <div class="field-group">
                <input type="submit" value="Save" class="button">
            </div>
            <br>
            <div class="view">
                <label for="name">Monitored space keys:</label>
                <div class="field-value">
                    $!affectedSpaces
                </div>
            </div>

            <div class="view">
                <label for="name">Affected user groups:</label>
                <div class="field-value">
                    $!affectedGroups
                </div>
            </div>

            <div class="view">
                <label for="name">Period to consider pages as outdated (day(s)):</label>
                <div class="field-value">
                    $!timeframe
                </div>
            </div>

            <div class="Info">
                <br>
                <strong>Configuration:</strong> visit Administration - Scheduled Jobs in order to set time interval for running job or to run it manually<br>
                <strong>Default schedule:</strong> each day at 00:00:00<br>
                <strong>Job:</strong> Old Pages Review Job<br>
            </div>

        </form>
    </body>
</html>